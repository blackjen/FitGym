<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FitGym">




        <activity
            android:name=".MainActivity">
        </activity>

        <activity
            android:name=".notifiche.NotificheActivity">
        </activity>

        <activity
            android:name=".corsi.CorsiActivity">
        </activity>

        <activity
            android:name=".NegozioActivity">
        </activity>

        <activity
            android:name=".utente.ProfiloActivity">
        </activity>

        <activity
            android:name=".accesso.ConfermaRegistrazioneActivity">
        </activity>

        <activity
            android:name=".accesso.RegistrazioneActivity">
        </activity>

        <activity
            android:name=".accesso.SchermataBenvenutoActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".accesso.LoginActivity">
        </activity>

    </application>

</manifest>